import { contentType } from '@optimizely/cms-sdk';

/**
 * Generic media
 */
export const GenericMediaCT = contentType({
  key: 'GenericMedia',
  displayName: 'Generic media',
  baseType: '_media'
});
